name: Update Data Core IA

on:
  schedule:
    - cron: "*/10 * * * *" # toutes les 10 minutes
  workflow_dispatch:

jobs:
  update-data-core:
    runs-on: ubuntu-latest

    steps:
      - name: 🚀 Appel de la fonction Base44 ingestGlobalData
        run: |
          echo "Lancement de la mise à jour du Data Core IA..."
          curl -X POST "https://macro-pulse-a4a78c96.base44.app/api/apps/68c96457d9c56fe1a4a78c96/functions/ingestGlobalData" \
          -H "Content-Type: application/json" \
          -d '{}'
